Research and understand what a `merge conflict` is and how to resolve it.
    1.) Merge conflicts are when 2 people are submiting edits on the same lines of code. Yuo can resolve the change through the command line. Or go through Github
    and select which branch changes you would like to keep and select resvolve conflicts

Reseach the Git commands `pull`, `rebase`, `merge`. These commands will allow you to bring in changes that other developers push to the master branch.
    2.) Pull - Update the local repository to the newest commits
        Rebase - you can use this to edit previous commit messages, combine multiple commits into one, delete or revert commits that arent 
        necessary
Reseach the Git commands `reset `, `revert`, `clean`. These commands will allow you to go back and ammends previous commits you have made.
    3.) git reset is there for undoing changes. -soft, --mixed, --hard.
    revert - undo command
    clean - another form of undoing
Research and set up a Graphical User Interface (GUI) Git console. 
    4.) done
Research and setup SSH keys with Github, so that you do not need to input your username/password each time you push. 
    5.) 